<!DOCTYPE html>
<html lang="es">
<!-- Include Head -->
<%- include('./partials/head.ejs') %>

<body>
	<!-- Header -->
	<% if (locals.nombre!=undefined) { %>
		<%- include('./partials/headerLogueado.ejs')%>
	<%} else { %>
		<%- include('./partials/header.ejs')%>
	<% } %>
	
    <!-- /Header -->
    <div class="container products-wrapper">
		<div class="row">
			<div class="col-12">

				<h2 class="products-rostro"><%= productos[0].seccion %> </h2>
            </div>
            <% for(i=0; i < productos.length; i++){ %>
				<div class="col-12 col-sm-6 col-lg-3 product">
					<section class="product-box">
						<a href="/product/<%=productos[i].id %>">
							<figure class="product-box_image">
								<img src="/images/products/<%= productos[i].img_url %>" alt="producto">
							</figure>
							<article class="product-box_data">
								<h2><%= productos[i].nombre_producto %></h2>
								<p> Marca: <%= productos[i].marca %></p>
								<p> Creado por: <%= productos[i].usuarioCreadores.nombre_usuario %></p>
								<p><%= productos[i].descripcion %></p>
								<p>Comentarios: <%=productos[i].comentarios.length%></p>
							</article>
						</a>
					</section>
				</div>
			<% } %>
			
		</div>
	</div>

            
    